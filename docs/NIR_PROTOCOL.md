# NIR Protocol

Moved to [`../nir_protocol/protocol.md`](../nir_protocol/protocol.md) —
kept there rather than under `docs/` because it lives alongside
`nir_protocol/examples/` and is versioned together with any future
firmware-side protocol code in that same directory.
